spring:
  datasource:
        url: jdbc:mysql://localhost:3306/guo?useUnicode=true&characterEncoding=utf8
        username: root
        password: guoyibin
        driver-class-name: com.mysql.cj.jdbc.Driver

server:
   port: 9090



mybatis:
  mapper-locations: classpath:mapper/*.xml
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
files:
   upload:
     path: E:/files/